# Indra architecture

Authoritative design: `docs/BUILD_SPEC.md` §4 (two-process topology, .indra bundle format,
job system, cache, memory budgets). This document grows with implementation notes that the
spec does not cover; it does not duplicate the spec.

## Implemented so far

- (Phase 0 in progress — nothing implemented yet)
